# V3 扩展入口

核心流程只提供角色、场景、出镜、资产候选和双语剧本。报告整理与美术提示词属于后续可选扩展。

扩展应：

- 读取 `data/catalogs/catalog.json` 或当前单章结构记录。
- 明确声明支持的 `schema_version`。
- 写入自己的目录或新的用户交付文件。
- 不修改英文原稿、单章事实或全局目录。
- 不随核心逐章流程自动执行。

美术提示词扩展只能在用户确认资产范围并提供设计要求后运行；机器目录不包含审美决定。
